import IndisputableMonolith.Ethics.LLMAlignment.Stage3Adversarial

/-!
# LLM alignment theorem surface

This import surface collects theorem-backed claims for the open-weights LLM
alignment lane. The first landed certificate is the Stage 3 adversarial result:
ledger labels, not sigma alone, decide sigma-adversarial truth-debt pairs; the
governed `jratio_dpo mu=0.02` operating point passes the recorded hard-truth
gate while preserving a low policy-drift trust region.
-/
